import React, { ReactElement } from 'react';

/**
 * Pre-release demo
 */
export const Component = (): ReactElement => {
  return <div>Component (0.2.0 beta fix #1)</div>;
};

export default Component;
