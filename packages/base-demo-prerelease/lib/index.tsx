import React, { ReactElement } from 'react';

/**
 * Pre-release demo
 */
export const Component = (): ReactElement => {
  return <div>Component (Pre-release: test preid to beta)</div>;
};

export default Component;
